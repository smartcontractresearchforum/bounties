name: New Bounty Route

on: issues

env:
  gh_organization: smartcontractresearchforum
  pb_bounties_contibutor_id:  58
  pb_bounties_core_id: 57
  pb_bounties_community_id: 55
  gh_project_token: ${{ secrets.BOUNTIES_PAT }}
  issue_id: ${{ github.event.issue.number }}  

jobs:
  route_bounty:  
    name: Route Bounty
    runs-on: ubuntu-latest
    if: contains( github.event.issue.title, '🆕Bounty' )
    steps:
      - name: Issue Parse
        id: issue_parse
        uses: peter-murray/issue-forms-body-parser@v2.0.0
        with: 
          github_token: ${{ env.gh_project_token }}
          issue_id: ${{ env.issue_id }}
          separator: '###'
          label_marker_start: '◾'
          label_marker_end: '◾'   

      - name: Select JQ Values
        id: jq-values
        run: |
          echo ${{ toJSON(steps.issue_parse.outputs.payload) }}
          echo "{ROLES}={ ${{ toJSON(steps.issue_parse.outputs.payload) }} }" >> $GITHUB_OUTPUT
      
      - name: Add To Project Boards
        uses: leonsteinhaeuser/project-beta-automations@v1.2.1  
        id: add-to-project-baords
        env:
          DEBUG_LOG: "true"
          key_roles: "ROLES"   
          value_role_level: ${{ steps.jq-values.outputs.ROLES }}
        with:
          gh_token: ${{ env.gh_project_token }}
          organization: ${{ env.gh_organization }}
          project_id: ${{ env.pb_bounties_core_id }}
          resource_node_id: ${{ github.event.issue.node_id }}          
          

      